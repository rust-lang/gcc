/* The multi-target plugin contract holds for any build: the plugin
   compiles against these headers, loads, and observes one stable
   target for the whole compilation.  */
/* { dg-do compile } */

int f (void)
{
  return 0;
}
